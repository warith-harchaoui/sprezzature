# Requirements for meta_from_ollama.py
#
# Install:
#   pip install -r scripts/requirements-meta-tags.txt
#
# Used for:
#   - click      : CLI parser; replaces argparse here so the user-facing
#                  --help is consistent with the rest of the Click-backed
#                  sprezzature scripts.
#   - requests   : Ollama HTTP API (POST /api/generate).
#   - langdetect : detect language from extracted page text so the
#                  generated <meta> tags match the page language.

click>=8.1
requests>=2.31.0
langdetect>=1.0.9
