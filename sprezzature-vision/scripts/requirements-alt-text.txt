# Requirements for alt_from_ollama.py
#
# Install:
#   pip install -r scripts/requirements-alt-text.txt
#
# Used for:
#   - click      : CLI parser; replaces argparse here so the user-facing
#                  --help is consistent with the rest of the Click-backed
#                  sprezzature scripts.
#   - requests   : Ollama HTTP API (POST /api/generate).
#   - langdetect : detect language from --in DOC / --context text so the
#                  output language matches the page without relying on
#                  shell locale.
#   - Pillow     : optional — downscale large images before sending to
#                  the model. The script remains functional without it.

click>=8.1
requests>=2.31.0
langdetect>=1.0.9
Pillow>=10.0.0
